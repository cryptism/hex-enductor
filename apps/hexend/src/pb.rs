//! The schema's generated types live in crates/hexen-proto, shared with
//! apps/presentation-rs; re-exported here so `crate::pb::hexen::v1`
//! keeps working throughout this crate.

pub use hexen_proto::hexen;
