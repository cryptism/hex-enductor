{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The editor window only. Presentation windows load plain http:// pages and get no IPC at all.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
